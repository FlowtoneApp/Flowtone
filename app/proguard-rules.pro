# Flowtone application-specific R8 rules.
#
# Keep rules must only be added for code that is actually accessed through
# reflection, JNI, serialization, or another mechanism R8 cannot analyze.
#
# Do not keep the entire application package.
